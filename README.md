# RAG Ассистент

Система Retrieval-Augmented Generation (RAG) для создания интеллектуальных ассистентов на основе векторного поиска и языковых моделей. Проект поддерживает работу с двумя API: OpenAI и GigaChat от Сбера.

## 🚀 Возможности

- **Два режима работы**: OpenAI API и GigaChat API
- **Векторный поиск**: Использование ChromaDB для семантического поиска по документам
- **Кеширование ответов**: Оптимизация производительности через кеширование частых запросов
- **Контекстные ответы**: Генерация ответов на основе релевантных документов из базы знаний
- **Интерактивный интерфейс**: Удобное консольное приложение для взаимодействия

## 📋 Требования

- Python 3.11 или выше
- API ключи для выбранного провайдера:
  - **OpenAI**: API ключ от OpenAI
  - **GigaChat**: Auth Key и RqUID от Сбера

## 🔧 Установка

1. **Клонируйте репозиторий** (или скачайте проект):
   ```bash
   git clone <repository-url>
   cd prompt-5_8-main
   ```

2. **Создайте виртуальное окружение**:
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение**:
   - Windows (PowerShell):
     ```powershell
     .\venv\Scripts\Activate.ps1
     ```
   - Windows (CMD):
     ```cmd
     venv\Scripts\activate.bat
     ```
   - Linux/Mac:
     ```bash
     source venv/bin/activate
     ```

4. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

## ⚙️ Настройка

1. **Скопируйте файл с примерами переменных окружения**:
   ```bash
   cp .env.example .env
   ```

2. **Отредактируйте файл `.env`** и заполните необходимые переменные:

   **Для работы с OpenAI (assistant_api)**:
   ```env
   OPENAI_API_KEY=your-openai-api-key-here
   ```

   **Для работы с GigaChat (assistant_giga)**:
   ```env
   GIGACHAT_AUTH_KEY=your-gigachat-auth-key-here
   GIGACHAT_RQUID=your-gigachat-rquid-uuid-here
   ```

   **Примечание**: Вы можете использовать оба провайдера одновременно, заполнив все переменные.

3. **Получение API ключей**:
   - **OpenAI**: https://platform.openai.com/api-keys
   - **GigaChat**: https://developers.sber.ru/portal/products/gigachat

## 🎯 Использование

### Режим OpenAI API

Запустите приложение для работы с OpenAI:

```bash
cd assistant_api
python app.py
```

### Режим GigaChat

Запустите приложение для работы с GigaChat:

```bash
cd assistant_giga
python app.py
```

### Команды в интерактивном режиме

- Введите ваш вопрос для получения ответа
- `stats` - показать статистику системы (количество документов, размер кеша и т.д.)
- `clear` - очистить кеш ответов
- `exit` или `quit` - выйти из приложения

## 📁 Структура проекта

```
prompt-5_8-main/
├── assistant_api/          # Реализация с OpenAI API
│   ├── app.py              # Главное приложение
│   ├── rag_pipeline.py     # RAG pipeline
│   ├── vector_store.py     # Работа с векторным хранилищем
│   ├── cache.py            # Кеширование ответов
│   ├── data/
│   │   └── docs.txt        # База знаний
│   └── evaluate_ragas.py   # Оценка качества системы
│
├── assistant_giga/         # Реализация с GigaChat API
│   ├── app.py              # Главное приложение
│   ├── rag_pipeline.py     # RAG pipeline
│   ├── gigachat_client.py  # Клиент для GigaChat API
│   ├── getaccesstoken.py   # Модуль получения access token
│   ├── vector_store.py     # Работа с векторным хранилищем
│   ├── cache.py            # Кеширование ответов
│   └── data/
│       └── docs.txt        # База знаний
│
├── chroma_db/              # Хранилище ChromaDB (создается автоматически)
├── .env                    # Переменные окружения (не коммитится)
├── .env.example            # Пример переменных окружения
├── .gitignore              # Игнорируемые файлы
├── requirements.txt        # Зависимости проекта
└── README.md               # Документация
```

## 🔍 Как это работает

1. **Загрузка документов**: При первом запуске система загружает документы из `data/docs.txt` и создает векторные представления (embeddings)

2. **Обработка запроса**:
   - Проверка кеша на наличие ответа на похожий вопрос
   - Если ответа нет в кеше:
     - Поиск релевантных документов через векторный поиск
     - Формирование промпта с контекстом из найденных документов
     - Генерация ответа языковой моделью
     - Сохранение ответа в кеш

3. **Возврат результата**: Пользователь получает ответ с указанием источника (кеш или модель)

## 🛠️ Модули

### `getaccesstoken.py` (assistant_giga)

Модуль для получения access token для GigaChat API. Может использоваться независимо:

```python
from getaccesstoken import get_access_token

token_data = get_access_token()
print(f"Token: {token_data['access_token']}")
print(f"Expires at: {token_data['expires_at']}")
```

## 📊 Оценка качества

Для оценки качества RAG системы используется RAGAS (Relevance Augmented Generation Assessment):

```bash
cd assistant_api
python evaluate_ragas.py
```

## 🔒 Безопасность

- **Никогда не коммитьте файл `.env`** в репозиторий
- Файл `.env` уже добавлен в `.gitignore`
- Используйте `.env.example` как шаблон для настройки

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Приветствуются предложения по улучшению! Создавайте issues и pull requests.

## 📚 Дополнительные ресурсы

- [OpenAI API Documentation](https://platform.openai.com/docs)
- [GigaChat API Documentation](https://developers.sber.ru/docs/ru/gigachat/api/overview)
- [ChromaDB Documentation](https://docs.trychroma.com/)
- [RAGAS Documentation](https://docs.ragas.io/)

## ⚠️ Известные ограничения

- Для работы с GigaChat требуется отключение проверки SSL сертификатов (`verify=False`)
- Размер контекста ограничен возможностями используемой языковой модели
- Кеш хранится локально в SQLite базе данных

## 🐛 Устранение неполадок

**Проблема**: Ошибка при получении access token для GigaChat
- **Решение**: Проверьте правильность `GIGACHAT_AUTH_KEY` и `GIGACHAT_RQUID` в файле `.env`

**Проблема**: Ошибка при работе с OpenAI API
- **Решение**: Убедитесь, что `OPENAI_API_KEY` установлен и имеет достаточный баланс

**Проблема**: Документы не загружаются
- **Решение**: Проверьте наличие файла `data/docs.txt` в соответствующей директории

---

**Приятного использования! 🎉**
